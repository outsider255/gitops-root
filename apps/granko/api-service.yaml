apiVersion: v1
kind: Service
metadata:
  name: granko-api
  namespace: granko
spec:
  selector:
    app: granko-api
  ports:
    - name: http
      port: 80
      # The image sets ASPNETCORE_URLS=http://+:8080 — above 1024 so the non-root `app` user can
      # bind it.
      targetPort: 8080
