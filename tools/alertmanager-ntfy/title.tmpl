{{ if eq .status "resolved" }}✅{{ else if eq .commonLabels.severity "critical" }}🔴{{ else }}🟠{{ end }} {{ .commonLabels.namespace | default "cluster" }}: {{ regexReplaceAll "([a-z])([A-Z])" .commonLabels.alertname "$1 $2" | trimPrefix "Kube " | trimPrefix "Node " | lower }}{{ $n := add (len .alerts) .truncatedAlerts }}{{ if gt $n 1 }} ×{{ $n }}{{ end }}{{ if eq .status "resolved" }} — resolved{{ end }}